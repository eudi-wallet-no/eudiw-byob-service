---
server:
  port: 9296

management:
  server:
    port: 9296
  endpoint:
    health:
      show-details: always

spring:
  application:
    environment: dev
  data:
    redis:
      host: localhost
      port: 9262
