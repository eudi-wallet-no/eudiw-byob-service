---
server:
  port: 9296

management:
  server:
    port: 9296
  endpoint:
    health:
      show-details: always

spring:
  config:
    import: optional:file:${EUDIW_BYOB_SERVICE_ENV}[.properties]
  application:
    environment: dev
  data:
    redis:
      host: localhost
      port: 9262

byob-service:
  api_key: ${BYOB_SERVICE_API_KEY}
